{
  "name": "Test-Repo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@axe-core/watcher": "^3.16.1",
    "@playwright/test": "^1.48.2",
    "@types/node": "^22.9.0"
  },
  "scripts": {
    "test-sample": "yarn playwright install chromium && yarn playwright install firefox && yarn playwright install webkit && npx playwright test login.spec.js --workers=9 --headed"
  },
  "dependencies": {
    "@axe-core/playwright": "^4.10.1",
    "dotenv-extended": "^2.9.0"
  }
}
